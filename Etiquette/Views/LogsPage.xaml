<Page
    x:Class="Etiquette.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Etiquette.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:LogLevelToColorConverter x:Key="LogLevelToColorConverter"/>
        <converters:StringVisibilityConverter x:Key="StringVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,24">
            <TextBlock Text="Journaux système" Style="{StaticResource TitleTextBlockStyle}"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="300"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Source des données :" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{x:Bind ViewModel.FileList}" 
                              SelectedItem="{x:Bind ViewModel.SelectedFile, Mode=TwoWay}"
                              HorizontalAlignment="Stretch"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="16" VerticalAlignment="Bottom">
                    <TextBlock Text="Afficher :" VerticalAlignment="Center" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,8,0"/>

                    <CheckBox Content="Info" IsChecked="{x:Bind ViewModel.ShowInfo, Mode=TwoWay}"/>
                    <CheckBox Content="Succès" IsChecked="{x:Bind ViewModel.ShowSuccess, Mode=TwoWay}"/>
                    <CheckBox Content="Alertes" IsChecked="{x:Bind ViewModel.ShowWarning, Mode=TwoWay}"/>
                    <CheckBox Content="Erreurs" IsChecked="{x:Bind ViewModel.ShowError, Mode=TwoWay}"/>

                    <AppBarSeparator/>

                    <Button Command="{x:Bind ViewModel.ClearLogsCommand}" ToolTipService.ToolTip="Effacer la session active">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Delete"/>
                            <TextBlock Text="Effacer"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </StackPanel>

        <ListView Grid.Row="1" 
                  ItemsSource="{x:Bind ViewModel.DisplayedLogs, Mode=OneWay}"
                  SelectionMode="None">
            <ListView.Header>
                <Grid Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Date / Heure" FontWeight="SemiBold" Grid.Column="0"/>
                    <TextBlock Text="Niveau" FontWeight="SemiBold" Grid.Column="1"/>
                    <TextBlock Text="Message" FontWeight="SemiBold" Grid.Column="2"/>
                </Grid>
            </ListView.Header>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,8" BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}" BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Timestamp}" Grid.Column="0" VerticalAlignment="Center" Opacity="0.8" FontSize="13"/>

                        <Border Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left"
                                Background="Transparent" CornerRadius="4" Padding="0">
                            <TextBlock Text="{Binding Level}" 
                                       Foreground="{Binding Level, Converter={StaticResource LogLevelToColorConverter}}" 
                                       FontWeight="Bold"/>
                        </Border>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding StackTrace}" 
                                       Visibility="{Binding StackTrace, Converter={StaticResource StringVisibilityConverter}}"
                                       FontSize="11" Foreground="#D13438" 
                                       TextWrapping="Wrap" Margin="0,4,0,0" FontFamily="Consolas"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

            <ListView.Footer>
                <TextBlock Text="Aucune donnée à afficher." 
                           HorizontalAlignment="Center" Margin="20" Opacity="0.5"
                           Visibility="{x:Bind ViewModel.DisplayedLogs.Count, Converter={StaticResource StringVisibilityConverter}, ConverterParameter=Reverse, Mode=OneWay}"/>
            </ListView.Footer>
        </ListView>
    </Grid>
</Page>